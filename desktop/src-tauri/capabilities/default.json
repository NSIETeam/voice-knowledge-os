{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local-only capability set",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{"name": "binaries/voice-memory-node", "sidecar": true, "args": true}]
    },
    "dialog:allow-open"
  ]
}
